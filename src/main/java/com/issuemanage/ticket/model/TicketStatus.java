package com.issuemanage.ticket.model;

public enum TicketStatus {
    OPEN,
    PENDING_ASSIGNMENT,
    ASSIGNED,
    IN_PROGRESS,
    ON_HOLD,
    RESOLVED,
    CLOSED,
    REOPENED,
    CANCELLED
}
